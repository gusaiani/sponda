django==5.1.6
tzdata==2025.2
djangorestframework==3.15.2
django-environ==0.12.0
django-cors-headers==4.6.0
psycopg2-binary==2.9.10
gunicorn==23.0.0
whitenoise==6.12.0
redis==5.2.1
requests==2.32.3
celery==5.6.3
sentry-sdk[django,celery]==2.20.0
openai==1.99.1

# Testing
pytest==8.3.4
pytest-django==4.9.0
factory-boy==3.3.1
playwright==1.49.1
pytest-playwright==0.6.2
pytest-timeout==2.3.1
pytest-rerunfailures==15.0
responses==0.25.6
# test_deploy_config.py parses .github/workflows/deploy.yml. It resolves today
# only because celery and responses happen to pull PyYAML in; declared here so
# the guard does not start failing the day that stops being true.
pyyaml==6.0.3
# Linter for the backend. Pinned because CI installs from this file and a
# floating version would turn a ruff release into a surprise red build.
# Ruleset lives in backend/ruff.toml.
ruff==0.16.3
