name: CI / Deploy

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "22"

      - name: Install Python dependencies
        run: pip install -r backend/requirements.txt

      - name: Install Playwright browsers
        run: python -m playwright install --with-deps chromium

      - name: Build frontend
        working-directory: frontend
        run: npm ci && npm run build

      - name: Run unit and integration tests
        working-directory: backend
        env:
          DJANGO_SECRET_KEY: test-secret-key-for-ci
          BRAPI_API_KEY: test-api-key-for-ci
        run: pytest -x -q --ignore=tests/test_e2e.py --ignore=tests/test_e2e_browser.py --ignore=tests/test_e2e_favorites.py --ignore=tests/test_e2e_auth.py

      - name: Run e2e tests
        working-directory: backend
        env:
          DJANGO_SECRET_KEY: test-secret-key-for-ci
          BRAPI_API_KEY: test-api-key-for-ci
          GOOGLE_CLIENT_ID: test-client-id
        run: pytest -x -q tests/test_e2e_browser.py tests/test_e2e_favorites.py tests/test_e2e_auth.py

  deploy:
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: 159.203.108.19
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -e
            cd /opt/sponda
            git fetch origin main
            git reset --hard origin/main
            source venv/bin/activate
            pip install -r backend/requirements.txt
            command -v n >/dev/null || npm install -g n
            n lts -y
            hash -r

            # Build Next.js frontend
            cd frontend
            npm ci
            npm run build
            cd /opt/sponda

            # Django migrations and data refresh
            export DJANGO_SETTINGS_MODULE=config.settings.production
            cd /opt/sponda/backend
            python manage.py migrate --noinput
            python manage.py collectstatic --noinput
            python manage.py refresh_ipca
            python manage.py refresh_tickers

            # Install systemd services and timers
            cp /opt/sponda/systemd/sponda-frontend.service /etc/systemd/system/
            cp /opt/sponda/systemd/sponda-refresh.service /etc/systemd/system/
            cp /opt/sponda/systemd/sponda-refresh.timer /etc/systemd/system/
            cp /opt/sponda/systemd/sponda-og-images.service /etc/systemd/system/
            cp /opt/sponda/systemd/sponda-og-images.timer /etc/systemd/system/
            systemctl daemon-reload
            systemctl enable --now sponda-refresh.timer
            systemctl enable --now sponda-og-images.timer

            # Update Apache: proxy sponda.capital to Next.js (port 3000) instead of Django (8710)
            sed -i 's|ProxyPass / http://127.0.0.1:8710/|ProxyPass / http://127.0.0.1:3000/|g' /etc/apache2/sites-enabled/*.conf
            sed -i 's|ProxyPassReverse / http://127.0.0.1:8710/|ProxyPassReverse / http://127.0.0.1:3000/|g' /etc/apache2/sites-enabled/*.conf
            apachectl configtest && systemctl reload apache2

            # Restart services
            systemctl restart sponda
            systemctl enable --now sponda-frontend
            systemctl restart sponda-frontend
